package org.spoofer.fluff.game;

import android.graphics.Rect;

public class CollisionEngine {
    private static final int SEARCH_AREA_MARGIN = 20;

/*
    protected Scenery getLocalScenery(Rect botLocation) {
        if (null == scene)
            return null;

        Rect searchArea = new Rect(botLocation);
        searchArea.inset(-SEARCH_AREA_MARGIN, -SEARCH_AREA_MARGIN);
        return scene.findScenery(searchArea);
    }
*/
}
